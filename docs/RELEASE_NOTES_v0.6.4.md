# rest-sample-cache-writer 0.6.4

This patch aligns the scheduled writer with `rust-java-rest:4.5.0` and
`java-rust-cache:0.7.4`. The packaged cache runtime now follows the clean native ABI `29/7/6/3`
provenance line.

Schedules, TTL rules, locks, SQL, Redis keys, and Java business mapping are unchanged.
