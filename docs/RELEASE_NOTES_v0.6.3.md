# rest-sample-cache-writer 0.6.3

`0.6.3` aligns the runnable writer with `rust-java-rest:4.4.1` and
`java-rust-cache:0.7.3`. The packaged native runtime now uses exact full-commit provenance.

Projection schedules, distributed locks, TTL validation, PostgreSQL access, and Java business code
are unchanged.
